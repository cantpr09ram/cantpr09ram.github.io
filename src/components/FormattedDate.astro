---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const formattedDate = new Intl.DateTimeFormat("en-US", {
	month: "short",
	day: "2-digit",
	year: "numeric",
}).format(date);
---

<time class="mono-date" datetime={date.toISOString()}>
  {formattedDate}
</time>
